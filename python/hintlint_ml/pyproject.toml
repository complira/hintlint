[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hintlint-ml"
version = "0.1.0"
description = "Advisory ML sidecar scaffold for HintLint."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
hintlint-ml = "hintlint_ml.__main__:main"
